unit UEntity.Tipo;

interface

type
  TTipo = class
    private
      FID: Integer;
      FValorPassagem: Double;

    public
      constructor Create(aJSON: String);
  end;

implementation

uses
  DataSet.Serialize,
  FireDAC.comp.Client;

{ TTipo }

constructor TTipo.Create(aJSON: String);
var
  xMemTable: TFDMemTable;
begin
 xMemTable := TFDMemTable.Create(nil);
  try
    xMemTable.LoadFromJSON(aJSON);
   FValorPassagem :=
  finally

  end;
end;

end.
